# CustomNameClassifier
Classify store names with deeplearning


### 0725
- 데이터셋 50000개로 LayoutLM 재학습. 기존 1000개의 결과물이 더 좋음
- yolov8m 모델 학습 완료, 준수한 성능을 보이나 좀 더 다듬을 필요가 있음
- YOLO 모델을 발전시키는 쪽으로 개발

### 0726_0120
- 커스텀 템플릿 3가지 추가, 폰트 7종 추가 및 이미지 내용 좀 더 실제 이미지와 유사하게 개선(generate.py 변경내역 참조)
- 데이터셋 7만개로 증가
- 커밋 후 yolov8L 모델로 학습 예정

### 0727
- yolov8L모델 학습 완료, 일부 DATE 클래스를 놓치는 경향
- loss weight 를 조정하여 추가 학습 진행중
- donut 모듈 pull 및 가상환경 분리, gitignore 추가
- *.pt 파일 업로드 제외(용량제한)

### 0728
- yolov8L 추가 학습 완료, 성능 변동 없음
- donut 모듈 학습 트러블슈팅 진행 중
- torch 모듈 내 distributetd_c10d.py에서 line 1671에 분산 처리 모듈 gloo로 고정

### 0828
- yolov11l, yolov8x 모델 테스트, 성능 큰 향상 없음.
- 레이아웃 4종 추가
- 후보정 로직 생성
- 레이아웃 3종 추가된 모델 학습 전 커밋

### 0830
- 레이아웃 추가하여 테스트한 결과 성능 향상 없음
- 기존 모델을 기반으로 후보정 로직 개발
    - YOLO에서 먼저 인식 -> 인식한 영역을 잘라서 검정 배경에 일정 간격으로 수직 정렬해 OCR에 한번에 넘김(따로넘기면 오래걸려서) -> 이미지 전체 OCR 인식 후 YOLO에서 인식한 영역 외 빠진 텍스트 및 위치 정렬 -> 빠뜨린 텍스트의 위치로부터 위아래로 10%~20% 패딩을 줘서 다시 YOLO로 재시도 -> 추가로 탐지한 영역 보정
- customOCR.ipynb에서 했던 원리대로 데이터 정제 로직 생성

### 0901
- Electron으로 웹 기반 애플리케이션 개발, Python으로 백엔드 처리, SQLite로 로컬 DB 저장 방식으로 전체 가닥을 잡음
- 기존 가계부 기능 + 딥러닝 서비스 추가하여 웹앱 기반 애플리케이션 개발

### 0904
- 카테고리 메뉴
    - 계좌 및 카테고리 추가/수정/삭제 기능을 구현하였으나 수정 시 거래내역에서 에러가 발생하여, UUID 도입. UUID 관련 코드로 모두 수정 필요
- 거래내역 메뉴
    - 필터 기능 및 드롭다운 입력 기능 구현
    - 열 추가/삭제 기능 구현, 저장 및 초기화 구현
    - 카테고리 메뉴와의 연결이 불안정하여, 카테고리 메뉴 안정화시킨 후 이어서 제작

### 0907
- 전체적인 함수 및 HTML 수정, 팝업 및 추가/수정/삭제 로직 안정화
- 화면자체는 정상적으로 출력되나, DB연계 과정에서 type 불일치 등 에러 발생
- 데이터가 모두 이름만을 기준으로 처리되고 있어, 계좌DB부터 id를 기준으로 처리하도록 수정 중
- 계좌 DB 수정 후 카테고리도 UUID를 기준으로 프런트에서 처리하도록 수정 필요
- 모두 수정된 후 다시 저장이 동작하는지 테스트 필요

### 0908
- ~~카테고리 UUID를 기준으로 처리하도록 수정, 대분류의 경우 첫글자를 따서 majorUuid로 처리~~
- 소분류에서 진짜 uuid 시스템 적용, 대분류와 소분류 테이블 분리, DB 시스템 완전히 재구성
- 백엔드 및 프런트 시스템 완전히 재구성 필요(database.py, account_utils.py까지 완료)
- 계좌 DB 초기화 로직 추가 필요 ( V )
> ???: "젠장, 망했다!"

### 0909
- 카테고리 메뉴 DB 시스템 재구성 완료(대분류 테이블, 소분류 테이블로 분리)
- 새 백엔드 api에 맞게 프런트 데이터 처리과정 수정
- 초기화 및 저장 로직 완성

### 0910
- 바뀐 카테고리 메뉴에 맞춰 거래내역 DB도 재구성
- 재구성된 백엔드 api에 맞춰 프런트 데이터 처리과정 수정
- 상단 툴박스와 행 제목은 고정하고 내용물만 스크롤되도록 개선
- 형광펜 기능 구현중 ( V )

### 0911
- 형광펜 기능+플래그, 배경 색상 기능 구현(관련된 벡엔드 코드 수정)
- 저장 수행 로직 개선(팝업 분기, 취소 시 하이라이트)
- 편집중 외부 클릭시 취소되던 문제 개선
- 팝업에 optional title 추가

### 0913
- Mapping 메뉴 추가, 관련 DB 신설, Sidebar 링크 추가
- 딥러닝 출력-카테고리 매핑 UI추가, 드래그 이동 구현
- 필터 팝업 component 폴더로 이동
- 거래내역 초기화 오류 수정(app.py - database.py간 함수병 일치)

### 0914
- OCR 보정 메뉴 추가
- 전체 CSS 스타일 통합: index.css를 기준으로 색상, 스타일이 공유될 수 있도록 전체 css파일 수정 필요( V )
- TODO: 거래내역 필터에서 다중 정렬 추가 필요( X ) -- 엑셀도 없음 이런건
- TODO: confirm이나 input, alert, destructive에서 esc 누르면 취소로 기능 ( V )

### 0915
- css스타일 통합 완료
- ESC 팝업닫기 구현
- 상호명-카테고리 룰베이스 매핑 구현: OCR보정 메뉴와 유사하나 대분류-소분류 선택을 드롭다운 리스트로 구현
- 코드 요청이 거의 한계에 다다라서, AI모델 젬미니2.5에서 지피티5미니로 변경
- TODO: 룰베이스 매핑 코드 참고해서 거래내역 리스트 팝업 개선 필요 ( V )

### 0916
- 거래내역 리스트 팝업으로 개선, 스타일 생성
- 날짜 선택 중 외부 클릭 시 렌더 버그 수정
- 달력 스타일 테마 통일

### 0918
- 업로드 모달 팝업 생성 및 백엔드로 전송 구현
- 딥러닝 자동입력 관련 백엔드 API 신설
- OCR -> DF 보정 로직 수정(토스UI 대응)
- 업로드 후 반환 값 표시 팝업 UI 제작중(minor_uuid를 기반으로 값을 읽어오지 못하고 있음)

### 0919
- 전체 백엔드 코드를 뜯어본 후 문제가 되는 부분 모두 수정
    - 딥러닝 모델이 id를 반환하도록 개선
    - 노트북 파일에 있던 재탐색 로직 추가
    - 날짜 보정 로직 백엔드에 추가
    - 자동보정/룰베이스 적용 분기 개선
    - 자동보정/룰베이스 PATCH API 추가
    - 기타 관련된 백엔드 코드 수정
- 일단 팝업 및 데이터 삽입, 보정 규칙 모두 동작
- 파일명 클릭 시 이미지 팝업 추가
- 계좌 변경 시 동일한 파일명은 같은 계좌로 적용하며 하이라이트 효과
- 삽입 클릭 시 빈 셀이 있을 경우 하이라이트 효과를 주며 경고 메시지 출력
- 더치페이 기능 구현중
    - 기능 열 생성 및 UI렌더 방식 재구성, 취소 기능 구현 및 정산금액 계산 가능
    - 데이터 처리 기능 구현 및 메모 생성

### 0923
- 더치페이 기능 완성
    - 행 추가 및 삭제, 자잘한 버그들 수정(border color, 경고 렌더, 계좌 동기화 등)
- 거래내역 폼 생성
    - 기본 UI 생성 및 달력 팝업, 날짜 선택 selectPopup 구현
    - 리스트 선택란 생성 및 유형 선택시 적절한 초기화 로직 구현
    - 금액 필드 서식 적용
    - 입력 시 거래내역 메뉴와 연계하여 새 거래내역 생성
- 그 외 변경사항
    - FloatingSelectPopup에서 선택되어 있는 항목으로 자동 스크롤 및 강조 표시
    - DatePickerOverrides.css에서 요일 행 가운데 정렬 서식 지정 및 한글화
- 수정필요사항
    - 딥러닝 자동입력에서 다른ui에서 날짜 생성 시 날짜형식이 통일되지 않음( V )

### 0924
- 거래내역 폼 고도화
    - 검색 기능 및 내계좌이체 기능 추가
    - 확장 패널 UI구성 및 각 기능 연결
    - 입력 시 거래내역 메뉴에서 스크롤 및 하이라이트 효과 구현, 종료 시 입력한 행의 수 계산
    - TODO: 달력 팝업 열 때 나머지 요소 밀리는 현상 수정 필요 ( V )
- 그 외 변경사항
    - 거래내역 메뉴 초기 진입시 맨 아래로 자동 스크롤
    - DatePickerOverrides.css 에서 div요소가 gap을 만드는 요소 해결(0924TODO)
    - ocr_service.py에서 통일된 날짜를 반환하도록 개선 (0923TODO)